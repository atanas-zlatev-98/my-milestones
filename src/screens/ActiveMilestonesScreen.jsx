import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ActiveMilestones() {
  return (
    <SafeAreaView>
      <Text>Active MileStones</Text>
    </SafeAreaView>
  );
}
